public class Operadores {
    public static void main(String[] args){

        // Operadores em Java

        int x = 10;
        int y = 2;
        int z;

        // z = x + y; (soma)
        // z = x - y; (subtração)
        // z = x * y; (multiplicação)
        // z = x / y; (divisão)
        // z =  x % y; (% vai resultar no resto de uma divisão ex: 11%2 = 1 (resto))

        // Operadores de Atribuição (Augmented Assignment Operators)

        // x += y; (é a mesma coisa que: x = x + y)
        // x -= y; (é a mesma coisa que: x = x - y)
        // x *= y; (é a mesma coisa que: x = x * y)
        // x /= y; (é a mesma coisa que: x = x / y)
        // x %= y; (é a mesma coisa que: x = x % y)

        // Operadores de incremento e decremento (Increment and Decrement Operators)

        // x++; (é a mesma coisa que: x = x + 1 (Aumenta de 1 em 1))
        // x--; (é a mesma coisa que: x = x - 1 (Diminui de 1 em 1))

        // Ordem das Operações [P-E-M-D-A-S]

        // double result = 3 + 4 * (7-5) / 2.0;
        // segue o mesmo padrão da matemática
    }
}
